At the start of each conversation, YOU MUST read all files linked in this document.

- See @package.json for dependencies and available commands.
- See @.prettierrc for formatting conventions.
- See @eslint.config.js for linting conventions.
- See @src/app.css for theme variables.
- See @.env.example for environment variables.
- See @vite.config.ts for unit testing configuration.
- See @playwright.config.ts for end-to-end testing configuration.

Before finishing your work, YOU MUST:

- Run `npm run format` and then `npm run lint`.
- Run `npm run test:unit`.
