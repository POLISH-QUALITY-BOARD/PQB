At the start of each conversation, read all files linked in this document.

- See [package.json](package.json) for dependencies and available commands.
- See [.prettierrc](.prettierrc) for formatting conventions.
- See [eslint.config.js](eslint.config.js) for linting conventions.
- See [src/app.css](src/app.css) for theme variables.
- See [.env.example](.env.example) for environment variables.
- See [vite.config.ts](vite.config.ts) for unit testing configuration.
- See [playwright.config.ts](playwright.config.ts) for end-to-end testing configuration.

Before finishing your work:

- Run `npm run format` and then `npm run lint`.
- Run `npm run test:unit`.
